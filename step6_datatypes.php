<?php
// 文字列 (string)
$name = "Taro";

// 整数 (int)
$age = 25;

// 小数 (float / double)
$height = 170.5;

// 真偽値 (bool)
$isStudent = true;

// NULL（値がない状態）
$nickname = null;

// 各変数を var_dump() で型と値を確認
var_dump($name);
var_dump($age);
var_dump($height);
var_dump($isStudent);
var_dump($nickname);
